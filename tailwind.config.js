/** @type {import('tailwindcss').Config} */
export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--ss-font-body)', 'sans-serif'],
        heading: ['var(--ss-font-heading)', 'sans-serif'],
      },
    },
  },
};
